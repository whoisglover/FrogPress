<% if current_user.user_type == "student" %>

<% end %>

<h1> Add a Class <h1><br>
  <%= form_for :classroom, url: classroom_index_path do |f| %>
  <%= f.text_field :title, placeholder: "Class Code" %>
  <%= f.submit "Submit" %>
  <% end %>